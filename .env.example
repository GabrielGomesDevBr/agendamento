# Configuração do Banco de Dados PostgreSQL
# IMPORTANTE: Substitua pelos seus valores reais
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_secure_password_here
DB_NAME=agendamento_db

# Configuração JWT
# IMPORTANTE: Use uma chave secreta forte e única em produção
# Gere com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_min_32_characters_here
JWT_EXPIRES_IN=24h

# Configuração do Servidor
NODE_ENV=development
PORT=3000

# Configuração de CORS
# Em produção, configure com o domínio real da sua aplicação
FRONTEND_URL=http://localhost:8080

# INSTRUÇÕES DE SEGURANÇA:
# 1. Copie este arquivo para .env
# 2. Substitua TODOS os valores de exemplo por dados reais
# 3. NUNCA commite o arquivo .env no Git
# 4. Use senhas fortes e chaves JWT únicas
# 5. Em produção, use variáveis de ambiente do servidor